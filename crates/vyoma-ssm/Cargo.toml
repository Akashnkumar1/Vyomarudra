[package]
name = "vyoma-ssm"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "E1→E2 bridge: fractal seed generating an SSM (sequence model) block's weights"

[dependencies]
candle-core = "0.8"
candle-nn = "0.8"
rand = "0.8"
anyhow = "1"

[[bin]]
name = "vyoma-ssm"
path = "src/main.rs"
